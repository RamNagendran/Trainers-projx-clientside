

export function contentTitle (text) {
    return (
        <div className="divider" >{text}</div>
    )
} 

export function smallDivider (text) {
    return (
        <div className="modalDivider" >{text}</div>
    )
} 